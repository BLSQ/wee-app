# Workshop slides

This branch holds the slide deck of the wee-app workshop, served by GitHub Pages.
`index.html` is a single self-contained file. `vercel.json` stops Vercel from deploying this branch.
